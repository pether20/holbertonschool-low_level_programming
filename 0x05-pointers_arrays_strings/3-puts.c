#include "main.h"

/**
 * _puts - print string
 * @str: pointer char
 *
 * Return: Value
 */
void _strlen(char *str)
{
	int i = 0;

	while (*(s + i) != '\0')
	{
		_putchar(*(s + i));
		i++;
	}
	_putchar('\n');
}
